const { default: Item } = require("antd/lib/list/Item")

describe('Login Flow', () => {
    beforeEach(() => {
        
    })

    it('successfully loads', () => {
        cy.visit('/settings')
    })
})